# 四插件审计 · 对照本源底层需求（2026-06-11）

> 逐插件把「用户原始需求」拆成验收点，标注：✅已实现并验证 / 🟡机制在但需 live 复验 / ⛔残留缺陷·下一步。
> 证据散见 `handoff_2026-06-11/`（HANDOFF / LIVE_TEST / REARCH）与 GitHub PR #47 / #48。

## ① dao-proxy-pro 9.9.261

| 需求 | 状态 | 说明 |
|------|------|------|
| #1 Cascade 显示全部模型；免费/Pro 一致 | ✅ | `source.js` `MODEL_UNLOCK` 拦 `GetUserSettings` 注入全量 109 模型，仅显示层、与 tier 无关 |
| #1 未路由模型走官方 Cascade | 🟡 | `shouldRoute=false` 原路透传；逻辑在，但「Pro 付费账号」未在真实 Pro 账号 live 验证（手上是 Free） |
| #2 仿 CC Switch 加渠道面板 | ✅ | `_EA_PRESETS` 14 家预设 + Provider CRUD |
| #2 连线面板右侧自动拉渠道模型 | 🟡 | `discover-models` 已实现；多 provider 并存场景未 live 复验 |
| #3 首加渠道默认路由 SWE 1.6 Fast | ✅ | `dao_router.hotAddProvider` 首 provider 自动建路由，不覆盖已有；单测 9/9 + 前端反馈文案 |
| #3 首通道=SWE 基础版仅通道检测+发消息反馈 | 🟡 | 通道检测路由存在；「发消息后给打通反馈」文案需在 live 再确认一次 |
| #4 后端与官方 100% 同步（工具/引用/子代理/提示词） | ✅ | 真实 DeepSeek key 三层 E2E：25 工具透传（含 `trajectory_search`/`skill`）、decode 44/44、完整 `route()` 管线回包保真 |

**残留**：真实 Pro 账号下的「未路由走官方」live 复验；三面板交互全程录屏复验。

## ② dao-vsix 1.0.9

| 需求 | 状态 | 说明 |
|------|------|------|
| 全功能面板（Sessions/Knowledge/Playbook/Secret）跟随账号 | ✅ | live 拉到 104 条真实会话；token 选择 bug 已修（仅 cog_ 走 v1，否则 auth1） |
| 中间面板渲染 | ✅ | 修掉三串联 bug（垃圾片段 SyntaxError / esc 调服务端 mpEsc / token 误选）→ 完整渲染 |
| 内嵌 git 多账号绑定板块 | ✅ | Git Auth tab 已并入主面板 |
| 内嵌内网穿透板块 | ✅ | 内网穿透 board 与隧道实时同步（URL/工作区/根目录） |
| 启动注入帛书规则板块（删旧注新） | ✅ | `devinFullInject` upsert：note-8fc33484，KB 16→17，正文 7758 字完整 |
| 启动注入工作区动态 MD（删旧注新） | ✅ | `Dao Workspace Server` 知识 + DAO_TOKEN secret + Playbook，upsert |
| 路由官方网页**自动登录同账号** | 🟡 | 机制=simpleBrowser 共享 session；需 live 复验「自动登录到同一账号」闭环 |
| 三账号同步（Desktop↔全能↔官网板） | 🟡 | 账号取自 Devin Desktop 登录态；同步闭环需 live 复验 |

**残留**：官网自动登录 live 复验；确认删旧覆盖所有残留知识库/Playbook。

## ③ dao-bridge 2.0.0

| 需求 | 状态 | 说明 |
|------|------|------|
| 专穿当前 Devin Desktop 工作区（非整机） | ✅ | 仅暴露当前 IDE 工作目录；`/api/health` 返回 `workspace:devin-remote` |
| 随 IDE 窗口启停 | ✅ | 启动自动起隧道；关窗隧道关闭（公网转 CF 530，下次=新通道） |
| 实时 MD 文档输出 | ✅ | `workspace.md`（公网 URL + Token + 工作区信息），供云端 Agent 接管 |
| QUIC 屏蔽兜底 | ✅ | `--protocol http2`；浏览器 UA 绕 CF 1010 |
| 用户登录**自己的 Cloudflare 账号**全链路 | ⛔→🟡 | 目前用 quick-tunnel（无需账号、URL 临时）。本轮接入真实 CF token（cfut_ 已验证 active）做命名隧道/稳定 URL 基础设施 |
| 工作区信息与 proxy-pro 提示词隔离板块同步 | ⛔ | 仅基础工作区信息；动态提取 Cascade 提示词中的工作区上下文待深化 |

**残留**：基于真实 CF 账号的命名隧道（稳定 URL）产品化；提示词级工作区提取。

## ④ devin-git-auth 2.0.0（本轮按用户指令暂缓深推）

| 需求 | 状态 | 说明 |
|------|------|------|
| 多账号绑定同一 GitHub App | ✅ | 16 账号已统一注册（`already registered`=目标态） |
| 面板/登录链/orgId/PAT 保存断开 | ✅ | 5 步登录链 16 账号 200；面板与 API 一致 |
| 多账号**共享同一仓库活跃协同** | ⛔ | 需刷新活跃 OAuth 连接（github.com 网页登录/设备码）。账号资源有 2 个测试 GitHub 账号 + TOTP 可用于此 |

## 跨插件 / 基础设施

- **第二主账号同步**：`zhouyoukang/devin-remote`（PAT2）此前落后（last push 06-07）→ 本轮与 `zhouyoukang1234-spec/devin-remote` main 对齐。✅
- **Cloudflare**：cfut_ token 已验证 success+active；尝试用测试 GitHub 账号（hdougle，TOTP）在本 VM 注册新 CF 账号取独立 token。
- **中文文件名经 relay 损坏**：已规避（ASCII 文件名 + base64 内容）。
