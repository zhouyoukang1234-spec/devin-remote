# 隐私 · 凭证（绝不入库）—— 道法自然·去彼取此
**/accounts.md
**/10_accounts.md
*账资*
**/secrets.json
**/git-pats.json
*.pem
*.key
.env
.env.*

# 依赖 / 构建产物
node_modules/
*.log

# cloudflared 二进制（自带打包用, 体积大不入库; 由 tools/fetch-cloudflared.js 拉取）
**/dao-bridge-ext/bin/cloudflared
**/dao-bridge-ext/bin/cloudflared.exe
**/dao-bridge-ext/bin/*.tgz
# 自包含 bundled VSIX（含二进制, 走 Release 分发, 不入库）
*-bundled.vsix
# 所有 VSIX 构建产物 — 走 GitHub Release, 不入库
*.vsix

# Python 运行缓存 — rt-flow/dao-export 脚本产物
__pycache__/
*.pyc

# PyInstaller 冻结产物（体积大, 由 build_exe.py 本地构建, 不入库）
**/agent-vm/dist/
**/agent-vm/build/
*.spec
*.exe

# vendored MCP 模块的构建产物（源码入库, 产物本地 npm/uv build, 不入库）
**/vendor/**/lib/
**/vendor/**/dist/
**/vendor/**/.venv/
*.tsbuildinfo

# rt-flow-mobile build artifact
addons/rt-flow-mobile/*.zip

# 各插件运行期/诊断产物 (原 plugins/.gitignore, 现全局生效于 core/ 与 addons/)
conn.json
diag*.js
_artifacts*/
_rtflow*/

# 外接第三方 MCP 源 (cloud/vm-replica/vendor, 按 VENDOR.md 固定 commit 拉取, 不入库)
cloud/vm-replica/vendor/mcp-servers/
cloud/vm-replica/vendor/playwright-mcp/

# 私有发行穿透配置 (含 token·勿入库)
addons/rt-flow-mobile/conn.json
