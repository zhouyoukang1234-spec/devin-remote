{
  "name": "dao-bridge-agent",
  "version": "1.0.0",
  "private": true,
  "description": "dao-bridge standalone backend: local HTTP server + Cloudflare quick tunnel (*.trycloudflare.com) for a local machine. Decentralized, zero-account, no relay Worker.",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js"
  }
}
